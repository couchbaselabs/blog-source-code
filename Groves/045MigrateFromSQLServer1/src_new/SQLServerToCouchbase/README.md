Make sure to create a bucket called "sqltocb", and create a primary index on it.

To switch between SQL and CB, change the WhichDatabase setting in Web.config between "SQLServer" and "CouchbaseServer".
